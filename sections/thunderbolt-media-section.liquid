<!-- Thunderbolt 4 PRO USB-C Cable Media Section -->
<!-- Google Fonts - Assistant -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Assistant:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

<!-- AOS Animation Library -->
<link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />
<script src="https://unpkg.com/aos@next/dist/aos.js"></script>

<div class="thunderbolt-media-section">
  <div class="background-elements">
    <div class="bg-circle bg-circle-1"></div>
    <div class="bg-circle bg-circle-2"></div>
    <div class="bg-circle bg-circle-3"></div>
    <div class="bg-lines"></div>
    <div class="floating-particles"></div>
  </div>
  
  <div class="media-container">
    <!-- Header Section -->
    <div class="benefits-header" data-aos="fade-up" data-aos-duration="800">
      <div class="section-eyebrow">{{ section.settings.eyebrow | default: 'ראה את הכבל בפעולה' }}</div>
      <h2 class="benefits-title">{{ section.settings.main_title | default: 'ביצועים שאתה <span class="highlight">יכול לראות</span>' }}</h2>
      <p class="section-description">{{ section.settings.description | default: 'צפה כיצד כבל Thunderbolt 4 PRO מעביר קבצים במהירות הבזק ומטעין את המכשירים שלך בכוח מלא' }}</p>
    </div>
    
    <!-- Media Content -->
    <div class="media-content" data-aos="fade-up" data-aos-delay="200">
      <div class="media-showcase">
        <div class="video-container">
          <video id="product-video" class="product-video" poster="{{ 'thunderbolt-video-poster.jpg' | asset_url }}" muted playsinline>
            <source src="{{ section.settings.video_url | default: 'thunderbolt-demo.mp4' }}" type="video/mp4">
            Your browser does not support the video tag.
          </video>
          
          <div class="video-overlay">
          <div class="video-controls">
            <button class="play-button" id="play-button">
                <div class="play-icon">
                  <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <polygon points="5 3 19 12 5 21 5 3"></polygon>
              </svg>
                </div>
                <div class="play-ripple"></div>
            </button>
              <div class="play-text">לחץ לצפייה</div>
            </div>
            
            <div class="video-stats">
              <div class="stat-item" data-aos="fade-up" data-aos-delay="400">
                <div class="stat-number">40</div>
                <div class="stat-unit">Gbps</div>
                <div class="stat-label">מהירות העברה</div>
              </div>
              <div class="stat-item" data-aos="fade-up" data-aos-delay="600">
                <div class="stat-number">100</div>
                <div class="stat-unit">W</div>
                <div class="stat-label">כוח טעינה</div>
              </div>
              <div class="stat-item" data-aos="fade-up" data-aos-delay="800">
                <div class="stat-number">4K</div>
                <div class="stat-unit">@60Hz</div>
                <div class="stat-label">רזולוציה</div>
              </div>
            </div>
          </div>
          
          <div class="video-glow"></div>
        </div>
        
        <!-- Performance Indicators -->
        <div class="performance-indicators" data-aos="fade-up" data-aos-delay="1000">
          <div class="indicator-grid">
            <div class="performance-item">
              <div class="performance-icon speed-icon">
                <svg viewBox="0 0 24 24" width="40" height="40" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <path d="M13 2L4 14H11L9 22L20 10H13L13 2Z" fill="#38d2d2"/>
                  <circle cx="10" cy="16" r="1" fill="white"/>
                  <circle cx="14" cy="6" r="1" fill="white"/>
                </svg>
              </div>
              <div class="performance-text">
                <div class="performance-title">העברה מהירה פי 8</div>
                <div class="performance-desc">מ-USB 2.0 רגיל</div>
              </div>
            </div>
            
            <div class="performance-item">
              <div class="performance-icon charging-icon">
                <svg viewBox="0 0 24 24" width="40" height="40" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <rect x="6" y="4" width="12" height="16" rx="2" fill="none" stroke="#38d2d2" stroke-width="1.5"/>
                  <path d="M10 8L14 8L11 12L13 12L9 16L12 12L10 12L10 8Z" fill="#38d2d2"/>
                </svg>
              </div>
              <div class="performance-text">
                <div class="performance-title">טעינה מהירה פי 5</div>
                <div class="performance-desc">מכבל רגיל</div>
              </div>
            </div>
            
            <div class="performance-item">
              <div class="performance-icon display-icon">
                <svg viewBox="0 0 24 24" width="40" height="40" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <rect x="2" y="4" width="20" height="14" rx="2" stroke="#38d2d2" stroke-width="1.5"/>
                  <path d="M8 21H16" stroke="#38d2d2" stroke-width="1.5"/>
                  <path d="M12 17V21" stroke="#38d2d2" stroke-width="1.5"/>
                  <rect x="5" y="7" width="14" height="8" fill="#38d2d2" opacity="0.3"/>
                </svg>
              </div>
              <div class="performance-text">
                <div class="performance-title">תמיכה ב-2 מסכים</div>
                <div class="performance-desc">4K במקביל</div>
              </div>
            </div>
          </div>
          </div>
        </div>
      </div>
      
    <!-- Call to Action -->
    <div class="media-cta" data-aos="fade-up" data-aos-delay="1200">
      <div class="cta-content">
        <h3 class="cta-title">חווה את ההבדל בעצמך</h3>
        <p class="cta-description">הזמן עכשיו וקבל משלוח חינם + אחריות לכל החיים</p>
        <div class="cta-buttons">
          <button class="hero-button hero-button-primary">
            <div class="button-background-effect"></div>
            <div class="button-content">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="button-icon">
                <circle cx="9" cy="21" r="1"></circle>
                <circle cx="20" cy="21" r="1"></circle>
                <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path>
              </svg>
              <span class="button-text">הזמן עכשיו</span>
            </div>
          </button>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .thunderbolt-media-section * {
    font-family: 'Assistant', sans-serif;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  
  .thunderbolt-media-section {
    background-color: #000000;
    color: #ffffff;
    padding: 120px 0;
    direction: {% if request.locale.iso_code == 'he' %}rtl{% else %}ltr{% endif %};
    position: relative;
    overflow: hidden;
  }
  
  .background-elements {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    overflow: hidden;
  }
  
  .bg-circle {
    position: absolute;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(56, 210, 210, 0.08) 0%, rgba(56, 210, 210, 0) 70%);
  }
  
  .bg-circle-1 {
    width: 800px;
    height: 800px;
    top: -400px;
    left: -300px;
    opacity: 0.6;
  }
  
  .bg-circle-2 {
    width: 600px;
    height: 600px;
    bottom: -300px;
    right: -200px;
    opacity: 0.4;
  }
  
  .bg-circle-3 {
    width: 400px;
    height: 400px;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    opacity: 0.2;
  }
  
  .bg-lines {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: linear-gradient(to right, rgba(56, 210, 210, 0.03) 1px, transparent 1px),
                      linear-gradient(to bottom, rgba(56, 210, 210, 0.03) 1px, transparent 1px);
    background-size: 50px 50px;
    opacity: 0.3;
  }
  
  .floating-particles {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: radial-gradient(2px 2px at 20px 30px, rgba(56, 210, 210, 0.3), transparent),
                      radial-gradient(2px 2px at 40px 70px, rgba(56, 210, 210, 0.2), transparent),
                      radial-gradient(1px 1px at 90px 40px, rgba(56, 210, 210, 0.4), transparent);
    background-repeat: repeat;
    background-size: 150px 100px;
    animation: float 20s linear infinite;
  }
  
  @keyframes float {
    0% { transform: translateY(0px) translateX(0px); }
    33% { transform: translateY(-10px) translateX(10px); }
    66% { transform: translateY(5px) translateX(-5px); }
    100% { transform: translateY(0px) translateX(0px); }
  }
  
  .media-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 20px;
    position: relative;
    z-index: 1;
  }
  
  .benefits-header {
    text-align: center;
    margin-bottom: 80px;
  }
  
  .section-eyebrow {
    font-size: 2.2rem;
    font-weight: 600;
    color: #38d2d2;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 20px;
  }
  
  .benefits-title {
    font-size: 6rem;
    font-weight: 800;
    margin-bottom: 30px;
    color: #ffffff;
    position: relative;
    display: inline-block;
    letter-spacing: -0.5px;
  }
  
  .highlight {
    color: #38d2d2;
    position: relative;
  }
  
  .highlight::after {
    content: '';
    position: absolute;
    bottom: -8px;
    right: 0;
    width: 100%;
    height: 4px;
    background: linear-gradient(90deg, #38d2d2, #2dd4bf);
    transform-origin: right;
    transform: scaleX(0);
    transition: transform 0.8s ease;
  }
  
  .benefits-header:hover .highlight::after {
    transform-origin: left;
    transform: scaleX(1);
  }
  
  .section-description {
    font-size: 2.4rem;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.8);
    max-width: 900px;
    margin: 0 auto;
    line-height: 1.6;
  }
  
  .media-content {
    margin-bottom: 80px;
  }
  
  .media-showcase {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 60px;
  }
  
  .video-container {
    position: relative;
    width: 100%;
    max-width: 1000px;
    border-radius: 25px;
    overflow: hidden;
    background: linear-gradient(145deg, rgba(25, 25, 25, 0.4), rgba(0, 0, 0, 0.9));
    border: 2px solid rgba(56, 210, 210, 0.3);
    box-shadow: 0 20px 60px rgba(56, 210, 210, 0.2);
  }
  
  .video-glow {
    position: absolute;
    top: -10px;
    left: -10px;
    width: calc(100% + 20px);
    height: calc(100% + 20px);
    border-radius: 30px;
    background: linear-gradient(45deg, rgba(56, 210, 210, 0.1), rgba(56, 210, 210, 0.05));
    z-index: -1;
    animation: pulse 3s ease-in-out infinite;
  }
  
  @keyframes pulse {
    0%, 100% { opacity: 0.3; transform: scale(1); }
    50% { opacity: 0.8; transform: scale(1.02); }
  }
  
  .product-video {
    width: 100%;
    height: auto;
    display: block;
    border-radius: 25px;
    position: relative;
    z-index: 2;
  }
  
  .video-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.4) 0%, rgba(0, 0, 0, 0.2) 100%);
    transition: all 0.4s ease;
    backdrop-filter: blur(2px);
    border-radius: 25px;
    z-index: 3;
  }
  
  .video-overlay.hidden {
    opacity: 0;
    pointer-events: none;
    transform: scale(0.95);
  }
  
  .video-controls {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    margin-bottom: 40px;
  }
  
  .play-button {
    background: linear-gradient(135deg, #38d2d2 0%, #2dd4bf 100%);
    border: none;
    cursor: pointer;
    color: #ffffff;
    width: 100px;
    height: 100px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 10px 40px rgba(56, 210, 210, 0.4);
    position: relative;
    overflow: hidden;
  }
  
  .play-button:hover {
    transform: scale(1.1);
    box-shadow: 0 15px 50px rgba(56, 210, 210, 0.6);
  }
  
  .play-icon {
    position: relative;
    z-index: 2;
    margin-left: 3px;
  }
  
  .play-ripple {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.3);
    transform: translate(-50%, -50%) scale(0);
    animation: ripple 2s ease-out infinite;
  }
  
  @keyframes ripple {
    0% { transform: translate(-50%, -50%) scale(0); opacity: 1; }
    100% { transform: translate(-50%, -50%) scale(2); opacity: 0; }
  }
  
  .play-text {
    font-size: 1.8rem;
    font-weight: 600;
    color: #ffffff;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
  }
  
  .video-stats {
    display: flex;
    gap: 40px;
    justify-content: center;
    flex-wrap: wrap;
  }
  
  .stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 20px;
    background: linear-gradient(145deg, rgba(25, 25, 25, 0.6), rgba(0, 0, 0, 0.8));
    border: 1px solid rgba(56, 210, 210, 0.3);
    border-radius: 15px;
    backdrop-filter: blur(10px);
    transition: all 0.4s ease;
  }
  
  .stat-item:hover {
    transform: translateY(-5px);
    border-color: rgba(56, 210, 210, 0.6);
    box-shadow: 0 10px 30px rgba(56, 210, 210, 0.2);
  }
  
  .stat-number {
    font-size: 3.2rem;
    font-weight: 800;
    color: #38d2d2;
    line-height: 1;
  }
  
  .stat-unit {
    font-size: 1.4rem;
    font-weight: 600;
    color: #38d2d2;
    margin-bottom: 5px;
  }
  
  .stat-label {
    font-size: 1.2rem;
    color: rgba(255, 255, 255, 0.8);
    font-weight: 500;
  }
  
  .performance-indicators {
    width: 100%;
    max-width: 900px;
  }
  
  .indicator-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
  }
  
  .performance-item {
    display: flex;
    align-items: center;
    gap: 20px;
    padding: 25px;
    background: linear-gradient(145deg, rgba(25, 25, 25, 0.3), rgba(0, 0, 0, 0.8));
    border: 1px solid rgba(56, 210, 210, 0.2);
    border-radius: 20px;
    transition: all 0.4s ease;
    position: relative;
    overflow: hidden;
  }
  
  .performance-item::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, transparent, rgba(56, 210, 210, 0.02), transparent);
    z-index: 1;
    transition: all 0.5s ease;
  }
  
  .performance-item:hover {
    transform: translateY(-5px);
    border-color: rgba(56, 210, 210, 0.4);
    box-shadow: 0 15px 40px rgba(56, 210, 210, 0.15);
  }
  
  .performance-item:hover::before {
    transform: translateX(100%);
  }
  
  .performance-icon {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(145deg, rgba(56, 210, 210, 0.1), rgba(56, 210, 210, 0.05));
    border: 2px solid rgba(56, 210, 210, 0.3);
    transition: all 0.4s ease;
    position: relative;
    z-index: 2;
  }
  
  .performance-item:hover .performance-icon {
    transform: scale(1.1) rotate(5deg);
    background: linear-gradient(145deg, #38d2d2, #2dd4bf);
  }
  
  .performance-text {
    flex: 1;
    z-index: 2;
    position: relative;
  }
  
  .performance-title {
    font-size: 1.8rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 5px;
    transition: all 0.3s ease;
  }
  
  .performance-item:hover .performance-title {
    color: #38d2d2;
  }
  
  .performance-desc {
    font-size: 1.4rem;
    color: rgba(255, 255, 255, 0.7);
    transition: all 0.3s ease;
  }
  
  .performance-item:hover .performance-desc {
    color: rgba(255, 255, 255, 0.9);
  }
  
  .media-cta {
    text-align: center;
    padding: 60px 40px;
    background: linear-gradient(145deg, rgba(25, 25, 25, 0.5), rgba(0, 0, 0, 0.8));
    border: 2px solid rgba(56, 210, 210, 0.2);
    border-radius: 25px;
    position: relative;
    overflow: hidden;
  }
  
  .media-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, transparent, rgba(56, 210, 210, 0.03), transparent);
    z-index: 1;
    animation: shimmer 4s ease-in-out infinite;
  }
  
  @keyframes shimmer {
    0% { transform: translateX(-100%); }
    50% { transform: translateX(100%); }
    100% { transform: translateX(100%); }
  }
  
  .cta-content {
    position: relative;
    z-index: 2;
  }
  
  .cta-title {
    font-size: 3.2rem;
    font-weight: 800;
    margin-bottom: 15px;
    color: #ffffff;
  }
  
  .cta-description {
    font-size: 1.8rem;
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: 30px;
  }
  
  .cta-buttons {
    display: flex;
    gap: 20px;
    justify-content: center;
    flex-wrap: wrap;
  }
  
  .cta-button {
    padding: 18px 40px;
    border-radius: 50px;
    font-size: 1.8rem;
    font-weight: 700;
    border: none;
    cursor: pointer;
    transition: all 0.4s ease;
    position: relative;
    overflow: hidden;
  }
  
  .cta-button.primary {
    background: linear-gradient(145deg, #38d2d2, #2dd4bf);
    color: #000000;
    box-shadow: 0 10px 30px rgba(56, 210, 210, 0.3);
  }
  
  .cta-button.primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 40px rgba(56, 210, 210, 0.4);
  }
  
  .cta-button.secondary {
    background: transparent;
    color: #38d2d2;
    border: 2px solid #38d2d2;
  }
  
  .cta-button.secondary:hover {
    background: #38d2d2;
    color: #000000;
    transform: translateY(-3px);
  }
  
  /* Responsive Design */
  @media (max-width: 1200px) {
    .benefits-title {
      font-size: 5rem;
    }
    
    .indicator-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  @media (max-width: 768px) {
    .thunderbolt-media-section {
      padding: 60px 0;
    }
    
    .media-container {
      padding: 0 20px;
    }
    
    .benefits-header {
      margin-bottom: 50px;
    }
    
    .benefits-title {
      font-size: 4rem;
    }
    
    .section-description {
      font-size: 1.8rem;
      max-width: 100%;
    }
    
    .media-showcase {
      gap: 40px;
    }
    
    .video-container {
      max-width: 100%;
      margin: 0;
      border-radius: 20px;
      box-shadow: 0 15px 40px rgba(56, 210, 210, 0.15);
    }
    
    .product-video {
      border-radius: 20px;
    }
    
    .video-overlay {
      border-radius: 20px;
      padding: 20px;
    }
    
    .video-controls {
      margin-bottom: 0;
    }
    
    .play-button {
      width: 80px;
      height: 80px;
    }
    
    .play-text {
      font-size: 1.6rem;
    }
    
    .video-stats {
      display: none;
    }
    
    .indicator-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }
    
    .performance-item {
      padding: 20px;
      flex-direction: row;
      text-align: left;
    }
    
    .performance-icon {
      width: 60px;
      height: 60px;
      flex-shrink: 0;
    }
    
    .performance-title {
      font-size: 1.7rem;
    }
    
    .performance-desc {
      font-size: 1.3rem;
    }
    
    .media-cta {
      padding: 40px 20px;
      margin: 0 10px;
    }
    
    .cta-title {
      font-size: 2.5rem;
    }
    
    .cta-description {
      font-size: 1.6rem;
    }
    
    .cta-buttons {
      flex-direction: column;
      align-items: center;
      gap: 15px;
    }
    
    .cta-button {
      width: 100%;
      max-width: 280px;
      padding: 16px 30px;
      font-size: 1.6rem;
    }
  }
  
  @media (max-width: 480px) {
    .thunderbolt-media-section {
      padding: 40px 0;
    }
    
    .media-container {
      padding: 0 15px;
    }
    
    .benefits-header {
      margin-bottom: 40px;
    }
    
    .section-eyebrow {
      font-size: 1.8rem;
    }
    
    .benefits-title {
      font-size: 3rem;
      line-height: 1.1;
    }
    
    .section-description {
      font-size: 1.6rem;
      line-height: 1.5;
    }
    
    .media-showcase {
      gap: 30px;
    }
    
    .video-container {
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(56, 210, 210, 0.2);
    }
    
    .product-video {
      border-radius: 15px;
    }
    
    .video-overlay {
      border-radius: 15px;
      padding: 15px;
    }
    
    .video-controls {
      margin-bottom: 0;
    }
    
    .play-button {
      width: 70px;
      height: 70px;
    }
    
    .play-text {
      font-size: 1.4rem;
    }
    
    .video-stats {
      display: none;
    }
    
    .performance-indicators {
      width: 100%;
    }
    
    .performance-item {
      flex-direction: column;
      text-align: center;
      gap: 15px;
      padding: 20px 15px;
    }
    
    .performance-icon {
      width: 50px;
      height: 50px;
    }
    
    .performance-title {
      font-size: 1.5rem;
      line-height: 1.3;
    }
    
    .performance-desc {
      font-size: 1.2rem;
      line-height: 1.4;
    }
    
    .media-cta {
      padding: 30px 15px;
      margin: 0 5px;
      border-radius: 20px;
    }
    
    .cta-title {
      font-size: 2.2rem;
      line-height: 1.2;
    }
    
    .cta-description {
      font-size: 1.4rem;
      line-height: 1.4;
      margin-bottom: 25px;
    }
    
    .cta-button {
      max-width: 100%;
      padding: 15px 25px;
      font-size: 1.5rem;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Initialize AOS animations
    AOS.init({
      once: true,
      disable: 'phone'
    });
    
    // Video controls
    const video = document.getElementById('product-video');
    const playButton = document.getElementById('play-button');
    const videoOverlay = document.querySelector('.video-overlay');
    
    if (video && playButton && videoOverlay) {
      playButton.addEventListener('click', function() {
        if (video.paused) {
          video.play();
          videoOverlay.classList.add('hidden');
        } else {
          video.pause();
          videoOverlay.classList.remove('hidden');
        }
      });
      
      video.addEventListener('ended', function() {
        videoOverlay.classList.remove('hidden');
      });
      
      video.addEventListener('click', function() {
        if (!video.paused) {
          video.pause();
          videoOverlay.classList.remove('hidden');
        }
      });
      
      // Autoplay when in viewport
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting && entry.intersectionRatio > 0.5) {
            // Auto-play only if user hasn't interacted
            if (video.paused && !video.hasAttribute('data-user-interacted')) {
            video.play();
              videoOverlay.classList.add('hidden');
            }
          } else {
            if (!video.paused) {
            video.pause();
              videoOverlay.classList.remove('hidden');
            }
          }
        });
      }, { threshold: 0.5 });
      
      observer.observe(video);
      
      // Mark user interaction
      playButton.addEventListener('click', function() {
        video.setAttribute('data-user-interacted', 'true');
      });
    }
    
    // Parallax background effect
    const bgCircles = document.querySelectorAll('.bg-circle');
    
    window.addEventListener('mousemove', function(e) {
      let x = e.clientX / window.innerWidth;
      let y = e.clientY / window.innerHeight;
      
      bgCircles.forEach((circle, index) => {
        const factor = (index + 1) * 0.3;
        circle.style.transform = `translate(${x * 20 * factor}px, ${y * 20 * factor}px)`;
      });
    });
    
    // CTA button effects
    const ctaButtons = document.querySelectorAll('.cta-button');
    
    ctaButtons.forEach(button => {
      button.addEventListener('click', function() {
        // Add ripple effect
        const ripple = document.createElement('span');
        ripple.style.position = 'absolute';
        ripple.style.borderRadius = '50%';
        ripple.style.background = 'rgba(255, 255, 255, 0.6)';
        ripple.style.transform = 'scale(0)';
        ripple.style.animation = 'ripple 0.6s linear';
        ripple.style.left = '50%';
        ripple.style.top = '50%';
        ripple.style.width = '20px';
        ripple.style.height = '20px';
        ripple.style.marginLeft = '-10px';
        ripple.style.marginTop = '-10px';
        
        this.appendChild(ripple);
        
        setTimeout(() => {
          ripple.remove();
        }, 600);
      });
    });
  });
  
  // Add ripple animation
  const style = document.createElement('style');
  style.textContent = `
    @keyframes ripple {
      to {
        transform: scale(4);
        opacity: 0;
      }
    }
  `;
  document.head.appendChild(style);
</script>

{% schema %}
{
  "name": "Media Section",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "ראה את הכבל בפעולה"
    },
    {
      "type": "text",
      "id": "main_title",
      "label": "Main Title",
      "default": "ביצועים שאתה <span class=\"highlight\">יכול לראות</span>"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "צפה כיצד כבל Thunderbolt 4 PRO מעביר קבצים במהירות הבזק ומטעין את המכשירים שלך בכוח מלא"
    },
    {
      "type": "url",
      "id": "video_url",
      "label": "Video URL",
      "info": "MP4 format recommended"
    }
  ],
  "presets": [
    {
      "name": "Media Section",
      "category": "Custom"
    }
  ]
}
{% endschema %}