<!-- Thunderbolt 4 PRO USB-C Cable Urgency Section -->
<!-- Google Fonts - Assistant -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Assistant:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

<!-- AOS Animation Library -->
<link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />
<script src="https://unpkg.com/aos@next/dist/aos.js"></script>

<div class="thunderbolt-urgency">
  <div class="background-elements">
    <div class="bg-circle bg-circle-1"></div>
    <div class="bg-circle bg-circle-2"></div>
    <div class="bg-circle bg-circle-3"></div>
    <div class="bg-lines"></div>
    <div class="floating-particles"></div>
    <div class="urgency-pulse"></div>
  </div>
  
  <div class="urgency-container">
    <!-- Header Section -->
    <div class="benefits-header" data-aos="fade-up" data-aos-duration="800">
      <div class="section-eyebrow">{{ section.settings.eyebrow | default: 'מלאי מוגבל' }}</div>
      <h2 class="benefits-title">{{ section.settings.main_title | default: 'הזדמנות אחרונה לקבל את <span class="highlight">Thunderbolt 4 PRO</span>' }}</h2>
      <p class="section-description">{{ section.settings.description | default: 'בגלל הביקוש הגבוה, נותרו רק יחידות בודדות במלאי. הזמן עכשיו כדי להבטיח את הכבל שלך' }}</p>
    </div>
    
    <!-- Urgency Content -->
    <div class="urgency-content" data-aos="fade-up" data-aos-delay="200">
      <!-- Urgency Badge -->
      <div class="urgency-badge-container">
        <div class="urgency-badge">
          <div class="badge-icon">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="#ff3b30">
              <path d="M12 2L13.09 8.26L22 9L17 14L18.18 21L12 17.77L5.82 21L7 14L2 9L10.91 8.26L12 2Z"/>
            </svg>
          </div>
          <span>מלאי מוגבל</span>
          <div class="badge-pulse"></div>
        </div>
      </div>
      
      <!-- Stock Indicator -->
      <div class="stock-indicator" data-aos="fade-up" data-aos-delay="400">
        <div class="stock-header">
          <h3 class="stock-title">מלאי נותר</h3>
          <div class="stock-count-display">
            <span class="stock-count">{{ section.settings.stock_count | default: 37 }}</span>
            <span class="stock-label">יחידות</span>
          </div>
        </div>
        <div class="stock-bar-container">
          <div class="stock-bar">
            <div class="stock-progress" style="width: {{ section.settings.stock_percentage | default: 25 }}%"></div>
            <div class="stock-glow"></div>
          </div>
          <div class="stock-text">
            <span class="warning-text">⚠️ מלאי אוזל במהירות</span>
          </div>
        </div>
      </div>
      
      <!-- Countdown Timer -->
      <div class="countdown-timer" data-aos="fade-up" data-aos-delay="600">
        <div class="timer-header">
          <h3 class="timer-title">המבצע מסתיים בעוד:</h3>
        </div>
        <div class="timer-display">
          <div class="timer-unit">
            <div class="timer-number" id="hours">23</div>
            <div class="timer-label">שעות</div>
          </div>
          <div class="timer-separator">:</div>
          <div class="timer-unit">
            <div class="timer-number" id="minutes">45</div>
            <div class="timer-label">דקות</div>
          </div>
          <div class="timer-separator">:</div>
          <div class="timer-unit">
            <div class="timer-number" id="seconds">32</div>
            <div class="timer-label">שניות</div>
          </div>
        </div>
      </div>
      
      <!-- Urgency Benefits -->
      <div class="urgency-benefits" data-aos="fade-up" data-aos-delay="800">
        <div class="benefit-item">
          <div class="benefit-icon">
            <svg viewBox="0 0 24 24" width="28" height="28" fill="#38d2d2">
              <path d="M3 9L12 2L21 9V20A2 2 0 0 1 19 22H5A2 2 0 0 1 3 20V9Z"/>
              <polyline points="9 22 9 12 15 12 15 22"/>
            </svg>
          </div>
          <div class="benefit-text">
            <h3>משלוח מהיר</h3>
            <p>הזמנה היום - משלוח מחר</p>
          </div>
        </div>
        
        <div class="benefit-item">
          <div class="benefit-icon">
            <svg viewBox="0 0 24 24" width="28" height="28" fill="#38d2d2">
              <path d="M12 22C12 22 20 18 20 12V5L12 2L4 5V12C4 18 12 22 12 22Z"/>
            </svg>
          </div>
          <div class="benefit-text">
            <h3>אחריות 30 יום</h3>
            <p>החזרה ללא שאלות</p>
          </div>
        </div>
        
        <div class="benefit-item">
          <div class="benefit-icon">
            <svg viewBox="0 0 24 24" width="28" height="28" fill="#38d2d2">
              <path d="M20.59 13.41L13.42 20.58A2 2 0 0 1 12 21A2 2 0 0 1 10.58 20.58L3.41 13.41A2 2 0 0 1 3.41 10.59L10.58 3.42A2 2 0 0 1 12 3A2 2 0 0 1 13.42 3.42L20.59 10.59A2 2 0 0 1 20.59 13.41Z"/>
              <circle cx="12" cy="12" r="1"/>
            </svg>
          </div>
          <div class="benefit-text">
            <h3>מחיר מיוחד</h3>
            <p>חיסכון של ₪50 עכשיו</p>
          </div>
        </div>
      </div>
      
      <!-- Price Display -->
      <div class="price-display" data-aos="fade-up" data-aos-delay="1000">
        <div class="price-container">
          <div class="original-price">₪199</div>
          <div class="current-price">₪149</div>
          <div class="savings-badge">חיסכון ₪50</div>
        </div>
        <div class="price-note">מחיר מיוחד למשך זמן מוגבל</div>
      </div>
      
      <!-- CTA Buttons -->
      <div class="urgency-cta" data-aos="fade-up" data-aos-delay="1200">
        <button class="buy-now-button primary">
          <span class="button-text">הזמן עכשיו - ₪149</span>
          <div class="button-icon">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="currentColor">
              <path d="M5 12H19M19 12L12 5M19 12L12 19"/>
            </svg>
          </div>
          <div class="button-glow"></div>
        </button>
        <button class="buy-now-button secondary">
          <span class="button-text">הוסף לעגלה</span>
          <div class="button-icon">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="currentColor">
              <circle cx="9" cy="21" r="1"/>
              <circle cx="20" cy="21" r="1"/>
              <path d="M1 1H5L7.68 14.39A2 2 0 0 0 9.65 16H19.4A2 2 0 0 0 21.36 14.39L23 6H6"/>
            </svg>
          </div>
        </button>
      </div>
      
      <!-- Trust Indicators -->
      <div class="trust-indicators" data-aos="fade-up" data-aos-delay="1400">
        <div class="trust-item">
          <svg viewBox="0 0 24 24" width="16" height="16" fill="#38d2d2">
            <path d="M9 12L11 14L15 10M21 12C21 16.9706 16.9706 21 12 21C7.02944 21 3 16.9706 3 12C3 7.02944 7.02944 3 12 3C16.9706 3 21 7.02944 21 12Z"/>
          </svg>
          <span>תשלום מאובטח SSL</span>
        </div>
        <div class="trust-item">
          <svg viewBox="0 0 24 24" width="16" height="16" fill="#38d2d2">
            <path d="M20 6L9 17L4 12"/>
          </svg>
          <span>משלוח חינם</span>
        </div>
        <div class="trust-item">
          <svg viewBox="0 0 24 24" width="16" height="16" fill="#38d2d2">
            <path d="M12 22C12 22 20 18 20 12V5L12 2L4 5V12C4 18 12 22 12 22Z"/>
          </svg>
          <span>אחריות לכל החיים</span>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .thunderbolt-urgency * {
    font-family: 'Assistant', sans-serif;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  
  .thunderbolt-urgency {
    background-color: #000000;
    color: #ffffff;
    padding: 120px 0;
    direction: {% if request.locale.iso_code == 'he' %}rtl{% else %}ltr{% endif %};
    position: relative;
    overflow: hidden;
  }
  
  .background-elements {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    overflow: hidden;
  }
  
  .bg-circle {
    position: absolute;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(56, 210, 210, 0.08) 0%, rgba(56, 210, 210, 0) 70%);
  }
  
  .bg-circle-1 {
    width: 800px;
    height: 800px;
    top: -400px;
    left: -300px;
    opacity: 0.6;
  }
  
  .bg-circle-2 {
    width: 600px;
    height: 600px;
    bottom: -300px;
    right: -200px;
    opacity: 0.4;
  }
  
  .bg-circle-3 {
    width: 400px;
    height: 400px;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    opacity: 0.2;
  }
  
  .bg-lines {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: linear-gradient(to right, rgba(56, 210, 210, 0.03) 1px, transparent 1px),
                      linear-gradient(to bottom, rgba(56, 210, 210, 0.03) 1px, transparent 1px);
    background-size: 50px 50px;
    opacity: 0.3;
  }
  
  .floating-particles {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: radial-gradient(2px 2px at 20px 30px, rgba(56, 210, 210, 0.3), transparent),
                      radial-gradient(2px 2px at 40px 70px, rgba(56, 210, 210, 0.2), transparent),
                      radial-gradient(1px 1px at 90px 40px, rgba(56, 210, 210, 0.4), transparent);
    background-repeat: repeat;
    background-size: 150px 100px;
    animation: float 20s linear infinite;
  }
  
  .urgency-pulse {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 300px;
    height: 300px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(255, 59, 48, 0.1) 0%, transparent 70%);
    animation: urgencyPulse 3s ease-in-out infinite;
  }
  
  @keyframes float {
    0% { transform: translateY(0px) translateX(0px); }
    33% { transform: translateY(-10px) translateX(10px); }
    66% { transform: translateY(5px) translateX(-5px); }
    100% { transform: translateY(0px) translateX(0px); }
  }
  
  @keyframes urgencyPulse {
    0%, 100% { opacity: 0.3; transform: translate(-50%, -50%) scale(1); }
    50% { opacity: 0.6; transform: translate(-50%, -50%) scale(1.2); }
  }
  
  .urgency-container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 20px;
    position: relative;
    z-index: 1;
  }
  
  .benefits-header {
    text-align: center;
    margin-bottom: 60px;
  }
  
  .section-eyebrow {
    font-size: 2.2rem;
    font-weight: 600;
    color: #ff3b30;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin-bottom: 20px;
    animation: urgentBlink 2s ease-in-out infinite;
  }
  
  @keyframes urgentBlink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.7; }
  }
  
  .benefits-title {
    font-size: 5.5rem;
    font-weight: 800;
    margin-bottom: 30px;
    color: #ffffff;
    position: relative;
    display: inline-block;
    letter-spacing: -0.5px;
  }
  
  .highlight {
    color: #38d2d2;
    position: relative;
  }
  
  .highlight::after {
    content: '';
    position: absolute;
    bottom: -8px;
    right: 0;
    width: 100%;
    height: 4px;
    background: linear-gradient(90deg, #38d2d2, #2dd4bf);
    transform-origin: right;
    transform: scaleX(0);
    transition: transform 0.8s ease;
  }
  
  .benefits-header:hover .highlight::after {
    transform-origin: left;
    transform: scaleX(1);
  }
  
  .section-description {
    font-size: 2.2rem;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.8);
    max-width: 800px;
    margin: 0 auto;
    line-height: 1.6;
  }
  
  .urgency-content {
    background: linear-gradient(145deg, rgba(25, 25, 25, 0.3), rgba(0, 0, 0, 0.8));
    border: 1px solid rgba(56, 210, 210, 0.2);
    border-radius: 30px;
    padding: 60px 50px;
    text-align: center;
    backdrop-filter: blur(10px);
    position: relative;
    overflow: hidden;
  }
  
  .urgency-content::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, transparent, rgba(56, 210, 210, 0.02), transparent);
    z-index: 1;
    animation: shimmer 4s ease-in-out infinite;
  }
  
  @keyframes shimmer {
    0% { transform: translateX(-100%); }
    50% { transform: translateX(100%); }
    100% { transform: translateX(100%); }
  }
  
  .urgency-badge-container {
    margin-bottom: 40px;
    position: relative;
    z-index: 2;
  }
  
  .urgency-badge {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: linear-gradient(145deg, #ff3b30, #e60012);
    color: #ffffff;
    font-size: 1.6rem;
    font-weight: 700;
    padding: 12px 25px;
    border-radius: 25px;
    position: relative;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(255, 59, 48, 0.3);
  }
  
  .badge-icon {
    animation: spin 2s linear infinite;
  }
  
  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
  
  .badge-pulse {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 25px;
    animation: badgePulse 1.5s ease-in-out infinite;
  }
  
  @keyframes badgePulse {
    0%, 100% { opacity: 0; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.05); }
  }
  
  .stock-indicator {
    margin-bottom: 40px;
    position: relative;
    z-index: 2;
  }
  
  .stock-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
  }
  
  .stock-title {
    font-size: 2.2rem;
    font-weight: 700;
    color: #ffffff;
  }
  
  .stock-count-display {
    display: flex;
    align-items: baseline;
    gap: 8px;
  }
  
  .stock-count {
    font-size: 3.5rem;
    font-weight: 800;
    color: #ff3b30;
    text-shadow: 0 0 20px rgba(255, 59, 48, 0.5);
  }
  
  .stock-label {
    font-size: 1.6rem;
    color: rgba(255, 255, 255, 0.7);
  }
  
  .stock-bar-container {
    position: relative;
  }
  
  .stock-bar {
    height: 12px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 6px;
    margin-bottom: 15px;
    overflow: hidden;
    position: relative;
  }
  
  .stock-progress {
    height: 100%;
    background: linear-gradient(90deg, #ff3b30, #ff6b47);
    border-radius: 6px;
    position: relative;
    transition: width 0.5s ease;
  }
  
  .stock-glow {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
    animation: stockGlow 2s ease-in-out infinite;
  }
  
  @keyframes stockGlow {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(100%); }
  }
  
  .warning-text {
    font-size: 1.4rem;
    color: #ff3b30;
    font-weight: 600;
  }
  
  .countdown-timer {
    margin-bottom: 40px;
    position: relative;
    z-index: 2;
  }
  
  .timer-title {
    font-size: 2.2rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 25px;
  }
  
  .timer-display {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 20px;
  }
  
  .timer-unit {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 20px 25px;
    background: linear-gradient(145deg, rgba(56, 210, 210, 0.1), rgba(56, 210, 210, 0.05));
    border: 1px solid rgba(56, 210, 210, 0.3);
    border-radius: 15px;
    backdrop-filter: blur(10px);
    min-width: 100px;
  }
  
  .timer-number {
    font-size: 3.5rem;
    font-weight: 800;
    color: #38d2d2;
    text-shadow: 0 0 20px rgba(56, 210, 210, 0.5);
    margin-bottom: 5px;
  }
  
  .timer-label {
    font-size: 1.2rem;
    color: rgba(255, 255, 255, 0.7);
    font-weight: 500;
  }
  
  .timer-separator {
    font-size: 3rem;
    font-weight: 800;
    color: #38d2d2;
    animation: timerBlink 1s ease-in-out infinite;
  }
  
  @keyframes timerBlink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
  }
  
  .urgency-benefits {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
    margin-bottom: 40px;
    position: relative;
    z-index: 2;
  }
  
  .benefit-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 25px 20px;
    background: linear-gradient(145deg, rgba(25, 25, 25, 0.3), rgba(0, 0, 0, 0.8));
    border: 1px solid rgba(56, 210, 210, 0.2);
    border-radius: 15px;
    transition: all 0.4s ease;
    backdrop-filter: blur(10px);
  }
  
  .benefit-item:hover {
    transform: translateY(-5px);
    border-color: rgba(56, 210, 210, 0.4);
    box-shadow: 0 10px 30px rgba(56, 210, 210, 0.15);
  }
  
  .benefit-icon {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: linear-gradient(145deg, rgba(56, 210, 210, 0.1), rgba(56, 210, 210, 0.05));
    border: 1px solid rgba(56, 210, 210, 0.3);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 15px;
    transition: all 0.4s ease;
  }
  
  .benefit-item:hover .benefit-icon {
    transform: scale(1.1);
    background: linear-gradient(145deg, #38d2d2, #2dd4bf);
  }
  
  .benefit-item:hover .benefit-icon svg {
    fill: #ffffff;
  }
  
  .benefit-text h3 {
    font-size: 1.6rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 8px;
  }
  
  .benefit-text p {
    font-size: 1.3rem;
    color: rgba(255, 255, 255, 0.7);
  }
  
  .price-display {
    margin-bottom: 40px;
    position: relative;
    z-index: 2;
  }
  
  .price-container {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 20px;
    margin-bottom: 15px;
  }
  
  .original-price {
    font-size: 2.5rem;
    font-weight: 600;
    color: rgba(255, 255, 255, 0.5);
    text-decoration: line-through;
  }
  
  .current-price {
    font-size: 4rem;
    font-weight: 800;
    color: #38d2d2;
    text-shadow: 0 0 20px rgba(56, 210, 210, 0.5);
  }
  
  .savings-badge {
    background: linear-gradient(145deg, #ff3b30, #e60012);
    color: #ffffff;
    font-size: 1.4rem;
    font-weight: 700;
    padding: 8px 15px;
    border-radius: 20px;
    box-shadow: 0 5px 15px rgba(255, 59, 48, 0.3);
  }
  
  .price-note {
    font-size: 1.4rem;
    color: rgba(255, 255, 255, 0.7);
    font-style: italic;
  }
  
  .urgency-cta {
    display: flex;
    gap: 20px;
    justify-content: center;
    margin-bottom: 40px;
    position: relative;
    z-index: 2;
  }
  
  .buy-now-button {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    font-size: 1.8rem;
    font-weight: 700;
    padding: 18px 35px;
    border-radius: 50px;
    border: none;
    cursor: pointer;
    transition: all 0.4s ease;
    position: relative;
    overflow: hidden;
    min-width: 200px;
  }
  
  .buy-now-button.primary {
    background: linear-gradient(145deg, #38d2d2, #2dd4bf);
    color: #000000;
    box-shadow: 0 10px 30px rgba(56, 210, 210, 0.3);
  }
  
  .buy-now-button.primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 40px rgba(56, 210, 210, 0.4);
  }
  
  .buy-now-button.secondary {
    background: transparent;
    color: #38d2d2;
    border: 2px solid #38d2d2;
  }
  
  .buy-now-button.secondary:hover {
    background: #38d2d2;
    color: #000000;
    transform: translateY(-3px);
  }
  
  .button-glow {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transform: translateX(-100%);
    transition: transform 0.6s ease;
  }
  
  .buy-now-button:hover .button-glow {
    transform: translateX(100%);
  }
  
  .trust-indicators {
    display: flex;
    justify-content: center;
    gap: 30px;
    position: relative;
    z-index: 2;
  }
  
  .trust-item {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 1.3rem;
    color: rgba(255, 255, 255, 0.8);
    font-weight: 500;
  }
  
  /* Responsive Design */
  @media (max-width: 1200px) {
    .benefits-title {
      font-size: 4.5rem;
    }
    
    .urgency-benefits {
      grid-template-columns: 1fr;
      gap: 20px;
    }
  }
  
  @media (max-width: 768px) {
    .thunderbolt-urgency {
      padding: 80px 0;
    }
    
    .urgency-container {
      padding: 0 15px;
    }
    
    .benefits-title {
      font-size: 3.5rem;
    }
    
    .section-description {
      font-size: 1.8rem;
    }
    
    .urgency-content {
      padding: 40px 25px;
    }
    
    .stock-header {
      flex-direction: column;
      gap: 15px;
      text-align: center;
    }
    
    .timer-display {
      gap: 15px;
    }
    
    .timer-unit {
      padding: 15px 20px;
      min-width: 80px;
    }
    
    .timer-number {
      font-size: 2.5rem;
    }
    
    .urgency-cta {
      flex-direction: column;
      align-items: center;
    }
    
    .buy-now-button {
      width: 100%;
      max-width: 300px;
    }
    
    .trust-indicators {
      flex-direction: column;
      gap: 15px;
      align-items: center;
    }
  }
  
  @media (max-width: 480px) {
    .benefits-title {
      font-size: 2.8rem;
    }
    
    .section-description {
      font-size: 1.6rem;
    }
    
    .urgency-content {
      padding: 30px 20px;
    }
    
    .timer-display {
      gap: 10px;
    }
    
    .timer-unit {
      padding: 12px 15px;
      min-width: 70px;
    }
    
    .timer-number {
      font-size: 2rem;
    }
    
    .current-price {
      font-size: 3rem;
    }
    
    .price-container {
      flex-direction: column;
      gap: 10px;
    }
  }
</style>

{% schema %}
{
  "name": "Urgency Section Advanced",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "מלאי מוגבל"
    },
    {
      "type": "text",
      "id": "main_title",
      "label": "Main Title",
      "default": "הזדמנות אחרונה לקבל את <span class=\"highlight\">Thunderbolt 4 PRO</span>"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "בגלל הביקוש הגבוה, נותרו רק יחידות בודדות במלאי. הזמן עכשיו כדי להבטיח את הכבל שלך"
    },
    {
      "type": "text",
      "id": "stock_count",
      "label": "Stock Count",
      "default": "37"
    },
    {
      "type": "range",
      "id": "stock_percentage",
      "min": 5,
      "max": 95,
      "step": 5,
      "unit": "%",
      "label": "Stock Percentage",
      "default": 25
    }
  ],
  "presets": [
    {
      "name": "Urgency Section Advanced",
      "category": "Custom"
    }
  ]
}
{% endschema %}

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Initialize AOS animations
    AOS.init({
      once: true,
      disable: 'phone'
    });
    
    // Parallax background effect
    const bgCircles = document.querySelectorAll('.bg-circle');
    
    window.addEventListener('mousemove', function(e) {
      let x = e.clientX / window.innerWidth;
      let y = e.clientY / window.innerHeight;
      
      bgCircles.forEach((circle, index) => {
        const factor = (index + 1) * 0.3;
        circle.style.transform = `translate(${x * 20 * factor}px, ${y * 20 * factor}px)`;
      });
    });
    
    // Countdown Timer
    function startCountdown() {
      // Set countdown to 24 hours from now
      const now = new Date().getTime();
      const countdownTime = now + (24 * 60 * 60 * 1000); // 24 hours
      
      const timer = setInterval(function() {
        const currentTime = new Date().getTime();
        const distance = countdownTime - currentTime;
        
        if (distance < 0) {
          clearInterval(timer);
          // Reset to 24 hours when countdown ends
          startCountdown();
          return;
        }
        
        const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
        const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((distance % (1000 * 60)) / 1000);
        
        // Update timer display with animation
        updateTimerNumber('hours', hours);
        updateTimerNumber('minutes', minutes);
        updateTimerNumber('seconds', seconds);
      }, 1000);
    }
    
    function updateTimerNumber(id, value) {
      const element = document.getElementById(id);
      if (element && element.textContent != value.toString().padStart(2, '0')) {
        element.style.transform = 'scale(1.2)';
        element.style.color = '#ff3b30';
        
        setTimeout(() => {
          element.textContent = value.toString().padStart(2, '0');
          element.style.transform = 'scale(1)';
          element.style.color = '#38d2d2';
        }, 150);
      }
    }
    
    // Start countdown
    startCountdown();
    
    // Stock counter animation
    function animateStockCounter() {
      const stockCount = document.querySelector('.stock-count');
      const stockProgress = document.querySelector('.stock-progress');
    
      if (stockCount && stockProgress) {
        // Simulate decreasing stock
        let currentStock = parseInt(stockCount.textContent);
        
        setInterval(() => {
          if (Math.random() < 0.1 && currentStock > 10) { // 10% chance every interval
            currentStock--;
            stockCount.textContent = currentStock;
            
            // Update progress bar
            const percentage = (currentStock / 150) * 100; // Assuming 150 was original stock
            stockProgress.style.width = Math.max(percentage, 5) + '%';
            
            // Flash effect
            stockCount.style.color = '#ff3b30';
            stockCount.style.transform = 'scale(1.2)';
            
            setTimeout(() => {
              stockCount.style.color = '#ff3b30';
              stockCount.style.transform = 'scale(1)';
            }, 300);
          }
        }, 30000); // Check every 30 seconds
      }
    }
    
    // Start stock animation
    animateStockCounter();
    
    // Button hover effects
    const buttons = document.querySelectorAll('.buy-now-button');
    
    buttons.forEach(button => {
      button.addEventListener('click', function() {
        // Add ripple effect
        const ripple = document.createElement('span');
        ripple.style.position = 'absolute';
        ripple.style.borderRadius = '50%';
        ripple.style.background = 'rgba(255, 255, 255, 0.6)';
        ripple.style.transform = 'scale(0)';
        ripple.style.animation = 'ripple 0.6s linear';
        ripple.style.left = '50%';
        ripple.style.top = '50%';
        ripple.style.width = '20px';
        ripple.style.height = '20px';
        ripple.style.marginLeft = '-10px';
        ripple.style.marginTop = '-10px';
        
        this.appendChild(ripple);
        
        setTimeout(() => {
          ripple.remove();
        }, 600);
      });
    });
    
    // Urgency badge pulse effect
    const urgencyBadge = document.querySelector('.urgency-badge');
    if (urgencyBadge) {
      setInterval(() => {
        urgencyBadge.style.transform = 'scale(1.05)';
        setTimeout(() => {
          urgencyBadge.style.transform = 'scale(1)';
        }, 200);
      }, 3000);
    }
    
    // Price animation on scroll
    const priceDisplay = document.querySelector('.current-price');
    
    const priceObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const element = entry.target;
          element.style.animation = 'priceGlow 2s ease-in-out infinite';
          priceObserver.unobserve(element);
        }
      });
    }, { threshold: 0.5 });
    
    if (priceDisplay) {
      priceObserver.observe(priceDisplay);
    }
    
    // Trust indicators animation
    const trustItems = document.querySelectorAll('.trust-item');
    
    trustItems.forEach((item, index) => {
      item.style.animationDelay = `${index * 0.2}s`;
      item.style.animation = 'slideInUp 0.6s ease-out forwards';
    });
    
    // Benefit items hover effect
    const benefitItems = document.querySelectorAll('.benefit-item');
    
    benefitItems.forEach(item => {
      item.addEventListener('mouseenter', function() {
        const icon = this.querySelector('.benefit-icon');
        if (icon) {
          icon.style.animation = 'bounce 0.6s ease-in-out';
        }
      });
      
      item.addEventListener('mouseleave', function() {
        const icon = this.querySelector('.benefit-icon');
        if (icon) {
          icon.style.animation = 'none';
        }
      });
    });
  });
  
  // Add additional animations
  const style = document.createElement('style');
  style.textContent = `
    @keyframes ripple {
      to {
        transform: scale(4);
        opacity: 0;
      }
    }
    
    @keyframes priceGlow {
      0%, 100% { 
        text-shadow: 0 0 20px rgba(56, 210, 210, 0.5);
        transform: scale(1);
      }
      50% { 
        text-shadow: 0 0 30px rgba(56, 210, 210, 0.8);
        transform: scale(1.05);
      }
    }
    
    @keyframes slideInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    @keyframes bounce {
      0%, 20%, 50%, 80%, 100% {
        transform: translateY(0);
    }
      40% {
        transform: translateY(-10px);
      }
      60% {
        transform: translateY(-5px);
      }
    }
  `;
  document.head.appendChild(style);
</script>